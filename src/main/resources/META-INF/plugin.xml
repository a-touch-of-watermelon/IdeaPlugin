<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>me.watermelon.a-touch-of-watermelon</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>Auto Some</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="ct-group@qq.com">ct-group</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
        The plugin functions are as follows:
        <ul>
            <li>every time after opening the project, automatically fetch all remote changes from all repositories once(support git and svn)</li>
            <li>open the IDE and after entering the welcome screen, automatically check for updates of the IDE and plugins once(once a day)</li>
            <li>more features in development...</li>
        </ul>
        <br>
        插件功能如下：
        <ul>
            <li>每次打开项目后，自动获取所有仓库远程变动一次（支持git和svn）</li>
            <li>打开IDE，进入欢迎界面后，自动检查IDE和插件的更新一次（一天一次）</li>
            <li>更多功能开发中。。。</li>
        </ul>
      ]]>
    </description>

    <change-notes><![CDATA[
        <ul>
            <li>[自动检查IDE和插件更新]修改为一天只检查一次</li>
        </ul>
      ]]>
    </change-notes>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>Git4Idea</depends>
    <depends>Subversion</depends>

    <extensions defaultExtensionNs="com.intellij">
        <postStartupActivity implementation="me.ct.group.activity.CtGroupProjectActivity"/>
    </extensions>

    <applicationListeners>
        <listener class="me.ct.group.listener.CheckUpdate" topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>
    
    <actions>
    </actions>
</idea-plugin>